nav.navigation
  router-link.navigation__logo(to='/')
    Icon(name='')
  .navigation__links
    router-link.navigation__link(:to='link.path' v-for='(link, i) in props', :key='i' v-if='link.navigation' v-html='link.name')
  .navigation__button(@click='openModal')
